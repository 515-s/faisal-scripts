--// Services
local workspace = game:GetService("Workspace")

local TARGET_NAME = "Common"

local function createLabel(obj)
    local main
    if obj:IsA("Model") then
        main = obj.PrimaryPart or obj:FindFirstChildWhichIsA("BasePart")
    elseif obj:IsA("BasePart") then
        main = obj
    end
    if not main then return end

    -- تحقق إذا النص موجود بالفعل لتجنب التكرار
    if main:FindFirstChild("CommonLabel") then return end

    local billboard = Instance.new("BillboardGui")
    billboard.Name = "CommonLabel"
    billboard.Adornee = main
    billboard.Size = UDim2.new(0,100,0,40)
    billboard.StudsOffset = Vector3.new(0,5,0)
    billboard.AlwaysOnTop = true
    billboard.Parent = main

    local label = Instance.new("TextLabel", billboard)
    label.Size = UDim2.new(1,0,1,0)
    label.BackgroundTransparency = 1
    label.Text = "Common"
    label.TextColor3 = Color3.fromRGB(0,255,0)
    label.TextScaled = true
    label.Font = Enum.Font.SourceSansBold
end

--// فحص كل العناصر داخل Workspace recursively
local function checkAll(parent)
    for _, obj in ipairs(parent:GetDescendants()) do
        if obj.Name == TARGET_NAME then
            createLabel(obj)
        end
    end
end

-- فحص كل Workspace أولًا
checkAll(workspace)

-- يراقب أي عنصر جديد يضاف
workspace.DescendantAdded:Connect(function(obj)
    if obj.Name == TARGET_NAME then
        createLabel(obj)
    end
end)
